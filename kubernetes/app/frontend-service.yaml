apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: fit-tracker-system
  labels:
    app.kubernetes.io/name: fit-tracker
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fit-tracker
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
